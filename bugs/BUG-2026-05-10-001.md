---
artifact: bug_record
metadata_schema_version: "1.0"
artifact_version: "1.0.0"
project: "tubeflow-flutter"
created: "2026-05-10"
updated: "2026-05-10"
status: active
source_skill: "sf-fix"
scope: "bug"
owner: "Diane"
bug_id: "BUG-2026-05-10-001"
title: "YouTube connection check runs before Flutter web Convex auth is ready"
bug_status: "fix-attempted"
severity: "high"
reported_by: "sf-fix"
first_observed: "2026-05-10"
last_observed: "2026-05-10"
environment: "Flutter web"
reproducibility: "unknown"
redaction_status: "not-required"
related_bugs: []
related_artifacts:
  - "tubeflow_app/lib/convex/convex_client.dart"
  - "tubeflow_app/lib/providers/providers.dart"
confidence: high
risk_level: medium
security_impact: yes
docs_impact: none
evidence:
  - "User report: Failed to check YouTube connection: Bad state: ConvexClient not initialized. Call ConvexClient.initialize() first."
  - "User retest report: Failed to check YouTube connection: Bad state: Missing Convex auth token for HTTP query bridge (youtube:getYoutubeConnectionStatus)."
depends_on: []
supersedes: []
next_step: "/sf-test --retest BUG-2026-05-10-001"
---

# Bug Record: BUG-2026-05-10-001

## Summary

- Title: YouTube connection check runs before Flutter web Convex auth is ready
- Current status: `fix-attempted`
- Severity: `high`
- Owner: Diane
- Next command: `/sf-test --retest BUG-2026-05-10-001`

## Reproduction

- Preconditions:
  - Flutter web app is running with `CONVEX_URL` and Clerk configured.
  - A user reaches a feature route that watches `youtubeConnectionProvider`.
- Steps:
  1. Open a YouTube-dependent route such as `/playlists`, `/videos`, `/notes`, or `/play`.
  2. Let the app check `youtube:getYoutubeConnectionStatus`.
  3. Trigger an HTTP bridge failure or missing auth-token state during the check.

## Expected Behavior

- TubeFlow checks the YouTube connection only after Clerk/Convex auth is ready. If the user is signed out or the Convex JWT is not ready yet, protected pages remain usable and treat YouTube as disconnected instead of rendering a full-page provider error.

## Observed Behavior

- Initial report: the web app reported `Failed to check YouTube connection: Bad state: ConvexClient not initialized. Call ConvexClient.initialize() first.`
- Retest report after the first fix: the web app still blocked all YouTube-dependent pages, including preferences navigation, with `Failed to check YouTube connection: Bad state: Missing Convex auth token for HTTP query bridge (youtube:getYoutubeConnectionStatus).`

## Evidence

- User report in chat, no secrets or personal data included.
- User retest report in chat, no secrets or personal data included.
- Code evidence: `ConvexService.initialize()` skips `ConvexClient.initialize()` on web, while `query`, `mutate`, and `action` fell back from the web HTTP bridge to `ConvexClient.instance`.
- Code evidence: `youtubeConnectionProvider` called `youtube:getYoutubeConnectionStatus` unconditionally, while other auth-sensitive providers already guard with `waitForConvexTokenReady()`.

## Diagnosis Notes

| Date UTC | Skill | Author | Hypothesis | Findings | Outcome |
|----------|-------|--------|------------|----------|---------|
| 2026-05-10 18:34:19 UTC | sf-fix | Codex | Flutter web should not fall back to `ConvexClient.instance` because the singleton is intentionally not initialized on web. | `ConvexService.initialize()` only initializes `ConvexClient` when `!kIsWeb`; `query`, `mutate`, and `action` catch HTTP bridge errors on web and then continue into the non-web client path. | supported |
| 2026-05-10 19:20:10 UTC | sf-auth-debug | Codex | `youtubeConnectionProvider` is firing before Clerk can mint the Convex JWT for the HTTP bridge. | The provider directly queried `youtube:getYoutubeConnectionStatus` without checking `authStateProvider` or `waitForConvexTokenReady()`. Preferences already used that guard pattern and local fallback. | supported |

## Fix Attempts

| Date UTC | Skill | Author | Changes | Validation | Result | Next Step |
|----------|-------|--------|---------|------------|--------|-----------|
| 2026-05-10 18:34:19 UTC | sf-fix | Codex | `tubeflow_app/lib/convex/convex_client.dart`: web query/mutation/action paths now log HTTP bridge failures and rethrow instead of falling back to the uninitialized `ConvexClient.instance`. | `flutter analyze` | passed | `/sf-test --retest BUG-2026-05-10-001` |
| 2026-05-10 19:20:10 UTC | sf-auth-debug | Codex | `tubeflow_app/lib/providers/providers.dart`: `youtubeConnectionProvider` now returns a disconnected fallback when the user is unauthenticated or the Clerk Convex JWT is not ready, matching the existing preferences fallback pattern. | `flutter analyze` | passed | `/sf-test --retest BUG-2026-05-10-001` |

## Retest History

| Date UTC | Skill | Retest Command | Environment | Result | Notes |
|----------|-------|----------------|-------------|--------|-------|

## Related Bugs / Artifacts

- Related bugs:
  - none
- Related artifacts:
  - `tubeflow_app/lib/convex/convex_client.dart`
  - `tubeflow_app/lib/providers/providers.dart`

## Lifecycle Notes

Canonical bug lifecycle statuses:

- `open`
- `needs-info`
- `needs-repro`
- `in-diagnosis`
- `fix-attempted`
- `fixed-pending-verify`
- `closed`
- `closed-without-retest`
- `duplicate`
- `wontfix`

Status transitions must stay explicit in this dossier and in `BUGS.md`.

## Redaction Status

- Current redaction status: `not-required`
- Redaction notes:
  - Evidence contains only a generic client error and file references.

## Closure Criteria

- `closed` requires at least one passing retest entry linked to the fix attempt.
- `closed-without-retest` requires an explicit reason plus residual risk.
- `duplicate` requires a linked canonical bug ID.
- `wontfix` requires a visible scope or product decision.

## Next Step

`/sf-test --retest BUG-2026-05-10-001`
